<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<ul>
  <li><%= link_to "Edit", edit_article_path(@article) %></li>
  <%= button_to 'Edit', edit_article_path(@article), :method => :get %>
  <%= button_to 'Delete post', @article, method: :delete, data: { confirm: 'Are you sure?' } %>
</ul>